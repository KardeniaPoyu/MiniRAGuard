项目：青居智选（租房合同 AI 审查微信小程序）

## 总览
- 前端：微信小程序（UniApp）
- 后端：Python 3.11 + FastAPI
- 后端代码目录：rent_agent_backend/

## 核心依赖与调用约定
- 使用 openai Python SDK 统一调用两个模型（兼容 Qwen-VL 与 DeepSeek）
- 所有外部 API 的 base_url 与 api_key **必须从 .env 读取**（禁止硬编码、禁止提交密钥）
- RAG 相关依赖：llama-index-core、chromadb、sentence-transformers

## 工程约束（写代码时必须遵守）
- 函数式风格：尽量使用纯函数/小函数组合，避免类封装（除非框架强制）
- 单一职责：每个文件只做一件事；跨文件通过清晰的函数边界协作
- 文档与类型：
  - 每个对外函数必须有 docstring（说明输入、输出、异常/边界）
  - 必须写类型标注（参数、返回值、关键中间结构）
- 配置与秘密：
  - `.env` 仅用于本地开发（不入库），线上通过环境变量注入
  - 禁止把任何 key、token、域名私密信息写入代码或文档

## 后端实现提示（与技术文档契约一致）
- 接口：
  - POST /api/analyze：接收 images(base64[])，多图循环抽取文本→拼接→缓存→RAG→审查 JSON→写缓存→返回
  - POST /api/chat：接收 question/context/history，构造 messages 调用模型返回自由文本 answer
- 数据契约：
  - risk_level 仅允许：高风险 / 中风险 / 低风险
  - legal_basis 固定格式：《法规名称》第X条

